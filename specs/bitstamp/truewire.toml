[project]
name = "bitstamp"

[spec]
dir = "spec"

[cores.default]
# Every endpoint here is the public v2 market data surface: no API key, no signature.
# `public` is the whole of what an endpoint has to tell a core (authoring rule 9).
meta = { type = "object", properties = { public = { type = "boolean" } }, additionalProperties = false }
